cd ~/ros2_ws/src/slam/maps && ros2 run nav2_map_server map_saver_cli -f map_01 --ros-args -p map_subscribe_transient_local:=true
- 2D 라이더 맵 저장 명령어


###저장한 3D 맵 보는 법###
ros2 run rtabmap_slam rtabmap \
  --ros-args \
  -p database_path:=$HOME/.ros/rtabmap.db \
  -p subscribe_rgbd:=false \
  -p subscribe_depth:=false \
  -p subscribe_scan:=false \
  -p subscribe_odom_info:=false \
  -p frame_id:=base_footprint \
  -p map_frame_id:=map

ros2 service call /publish_map rtabmap_msgs/srv/PublishMap "{global_map: true, optimized: true, graph_only: false}"
- 3D 매핑 재발행하는 명령어.(위의 서버를 실행하는 상태여야 함.)


###저장한 2D 맵 보는 법###
ros2 run nav2_util lifecycle_bringup map_server
- 2D 맵 전송 서버 실행

ros2 run nav2_map_server map_server --ros-args -p yaml_filename:=$HOME/ros2_ws/src/slam/maps/map_01.yaml
- 2D 맵 전송 명령어(위의 서버 명령어가 실행 상태여야 함.)


###카메라 관련###
/home/ubuntu/ros2_ws/src/full_Auto_Rover/saved_images - 사진 저장 위치

ros2 topic echo /full_auto_rover/event_status
ros2 topic echo /full_auto_rover/event_center
ros2 topic echo /full_auto_rover/event_aligned 
-> 확인 명령어

cd /home/ubuntu/ros2_ws
source /opt/ros/humble/setup.zsh
source install/setup.zsh
ros2 launch full_auto_rover full_auto_rover.launch.py -> 실행 명령어(위 세개는 코드 수정 시만)


